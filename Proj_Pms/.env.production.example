# Production Environment Variables for Vercel
# Copy these to Vercel Dashboard → Settings → Environment Variables

# Application URL (replace with your Vercel domain)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
NEXTAUTH_URL=https://your-app.vercel.app

# Database (Supabase Connection Pooling URL)
# Get from: Supabase Dashboard → Settings → Database → Connection Pooling
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres

# NextAuth Secret (Generate new for production)
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET=generate-a-new-secret-min-32-characters

# Email Service (Choose one option)
# Option 1: Gmail SMTP
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password

# Option 2: Resend API (for custom domain emails)
RESEND_API_KEY=re_your_api_key
EMAIL_FROM=noreply@yourdomain.com

# Environment
NODE_ENV=production
