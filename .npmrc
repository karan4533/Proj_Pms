# Use legacy peer deps to resolve nodemailer version conflict
legacy-peer-deps=true

# Suppress Next.js lockfile warning output
loglevel=error

# Increase timeout for OneDrive paths
fetch-timeout=60000
